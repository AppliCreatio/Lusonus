package com.example.emptyactivity.screens.ProfileScreen.classes

/**
 * Used to tell the application which kind of entry we are dealing with here.
 */
enum class MusicEntryTypes {
    PLAYLISTS,
    SONG
}